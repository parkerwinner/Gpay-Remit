# Template only — copy to secrets.yaml, fill in real values, and apply.
# Never commit the filled-in secrets.yaml to version control.
apiVersion: v1
kind: Secret
metadata:
  name: gpay-remit-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://gpay:CHANGE_ME@postgres:5432/gpay_remit?sslmode=disable"
  REDIS_URL: "redis:6379"
  STELLAR_NETWORK: "testnet"
  HORIZON_URL: "https://horizon-testnet.stellar.org"
